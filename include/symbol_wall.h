#ifndef SYMBOL_WALL_H
#define SYMBOL_WALL_H

#include <ostream>

/* Renders the '#' (wall) symbol only. */
void render_wall(std::ostream& out, double x0, double y0, double x1, double y1);

#endif /* SYMBOL_WALL_H */
