#ifndef SYMBOL_PATH_H
#define SYMBOL_PATH_H

#include <ostream>

/* Renders the '*' (solution path) symbol only. */
void render_path(std::ostream& out, double x0, double y0, double x1, double y1);

#endif /* SYMBOL_PATH_H */
